This is tool to quick type commands into the terminal 


Step to Install

- Clone the repo

> $: git clone https://github.com/symmetriccurve/autodev.git

- Clone the repo
> $: cd autodev

- install node_modules
> $: autodev> npm install

- install globally 

> $: autodev> npm install -g .
